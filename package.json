{
  "name": "chatbot",
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Run \"vercel dev\" directly from the terminal.'",
    "build": "echo 'Build step not needed for Vercel'",
    "start": "vercel start"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
